#include <vector>

using namespace std;

void mergesort(vector<int>);
vector<int> merge(vector<int>, vector<int>);

int main(int argc, char const *argv[])
{
	vector<int> arr = {10, 3, -1, 12, 0};
	mergesort(arr);
	return 0;
}

void mergesort(vector<int> arr) {






}

// merges two sorted arrays
vector<int> merge(vector<int>, vector<int>) {





}